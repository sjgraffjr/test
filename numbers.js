//old
var a = 1;
//new
const a = 1;
// const are immuttable, they can't change
let b = 2;
// let variable you can change, mutable {a use case of this may be in loops)
const multiple = 9 * 9;
const modulus = 10 % 4;
//varibale names should be in camelCase
const randNumber = math.random( 10 / 2);
function getRandomInt(0, 10) {
  min = Math.ceil(0);
  max = Math.floor(10);
  return Math.floor(Math.random() * (10 - 0 ) + 0; //The maximum is exclusive and the minimum is inclusive
}

function getRandom(0, 10) {
  return Math.random();
}

//whole number ex. 8 is an integer
// deicma ex 8.11111 is a float
